---
name: dota2-deep-match-review
description: >
  对 Dota 2 单场比赛进行证据驱动的专业复盘。用户说“复盘开始”、
  提供或选择 Match ID、追问同一场比赛的对线、时间点、团战、关键技能、
  核心责任、装备、输出、建筑、死亡、保枪或分锅时使用。支持通过项目中
  配置的 ashfury.cn 数据服务获取完整 OpenDota 解析数据。
---

# Dota 2 深度复盘

## 目标

复盘必须建立完整因果链：

`数据事实 → 当时行为 → 战术职责 → 直接后果 → 可执行改进`

结论可以直接，但强结论必须有证据。用户的明确要求优先于本 Skill 的默认结构。

## 触发与上下文

以下情况使用本 Skill：

- 用户说“复盘开始”、指定最近一把或第 N 把、提供 Match ID
- 用户要求分析 Dota 2 某场比赛或其中的时间点、团战、英雄和责任
- 已经开始复盘后继续追问装备、输出、关键技能、死亡、保枪或分锅

Match ID 已确定时，直接获取该场数据，不再要求用户提供比赛 URL。

同一场比赛的后续追问沿用当前 Match ID、用户身份和已取得的数据；除非解析状态变化或证据缺失，不重复获取、不重新做整场复盘。

## 项目配置边界

优先从当前项目记忆读取这些长期配置：

- 用户 Dota 2 Account ID
- 自建数据服务根地址
- 当前环境偏好的数据入口
- 服务端是否托管 Replay Parse
- 用户长期表达与责任判断偏好

这些值属于项目，不在 Skill 内维护第二份。接口路由、解析状态和失败处理见 [数据获取与证据层级](references/data-access.md)。

## 按任务读取参考文件

- 选择最近比赛、获取单场数据、判断解析完整度或处理接口失败：读取 [data-access.md](references/data-access.md)
- 默认完整复盘、“复盘开始 + Match ID”或用户要求重做完整分析：读取 [full-review.md](references/full-review.md)、[equipment-review.md](references/equipment-review.md) 和 [submission-protocol.md](references/submission-protocol.md)
- 针对已确定比赛追问某个时间点、团战、英雄、技能、装备、输出、死亡、保枪或分锅：读取 [focused-review.md](references/focused-review.md)；涉及装备、BKB、Aegis或买活时同时读取 [equipment-review.md](references/equipment-review.md)

完整复盘同时读取 `data-access.md`、`full-review.md`、`equipment-review.md` 和 `submission-protocol.md`。若当前环境可以运行 Python，必须先运行 `scripts/extract_match_facts.py` 生成统一事实账本；否则按提交协议手工建立同等账本。专项追问已有完整数据时，只需读取其对应参考文件；需要补取数据时再读取 `data-access.md`。

## 通用执行合同

### 1. 先确定证据等级

在分析前明确内部证据等级：

- `完整解析`：有 players、objectives、teamfights、经济/经验曲线和 version 等关键字段
- `部分解析`：有玩家与部分时间轴，但关键团战或曲线缺失
- `摘要`：只有 KDA、胜负、时长、最终装备等面板信息
- `录像级`：另有可观看 Replay 或可靠逐帧画面

只有完整解析才能称为完整数据复盘。摘要分析必须明确限制；OpenDota 完整解析也不能冒充逐帧录像。

### 2. 建立事实账本

分析中始终区分：

- `数据明确显示`：字段、事件和时间轴直接支持
- `高概率推断`：多项数据共同支持，但没有画面直接确认
- `无法确认`：缺少位置、技能时间戳、视野或录像证据

不能编造走位、鼠标操作、玩家意图、精确技能覆盖对象或技能当时必然可用。

完整复盘必须先完成事实账本和覆盖表，之后才允许成稿。边提取边写最终结论视为流程失败。

### 3. 禁止面板式评价

KDA、净资产、总伤害、死亡、承伤、治疗和建筑伤害都不能单独决定表现。必须结合英雄职责、资源优先级、输出对象、关键技能、装备窗口、团战存活、地图目标和买活转化。

评价核心问题是：

`这个英雄在这局应该创造什么胜利条件，实际完成了多少？`

### 4. 默认完整复盘的硬性覆盖

默认完整复盘必须覆盖：

- 三条线，并区分纯对线与10—15分钟补发育
- 双方辅助0—6分钟英雄伤害及其向核心发育的转化
- 双方各2—4个真正改变团战结构的关键技能
- 十名玩家的关键装备与强势窗口；六核完整出装路线、四辅助功能装、所有BKB持有者和每代Aegis单独审计
- 双方一、二、三号位六名核心的独立审计和同位置横向比较
- 约3—5波决定比赛走势的团战及其地图收益
- 用户个人表现、关键决策点和可执行改进
- 装备、伤害对象、建筑、Roshan/Aegis、买活和关键死亡
- 失败局责任排序；胜利局贡献、危险失误和被胜利掩盖的问题

任一项因数据不足无法完成时，明确指出限制；不能用“保持简洁”省略，也不能用最终面板替代过程分析。

覆盖过某个英雄或事件不等于完成该栏目。只有固定模板中的全部必填字段已填写，或逐项标明数据限制，才算通过验收。

### 5. 关键技能事实纪律

`teamfights[].players[].ability_uses` 只证明技能在该识别窗口内是否被记录。若整场使用次数高于团战窗口之和，只能确认存在窗口外释放。

没有精确时间戳和冷却证据时：

- 可以说“技能没有覆盖这波团战”
- 可以分析“技能管理或无技能接团”的整体问题
- 不得直接说“有大故意不放”

关键技能分析必须回答：它改变了谁的输出窗口，随后改变了哪些击杀、经济、Roshan、建筑或基地结果。

### 6. 对线事实纪律

对线评价优先使用5、10分钟经济、补刀、反补、经验、死亡和被迫离线；15分钟数据同时受打野、游走、击杀与塔影响，必须单列为对线后发育。

辅助不能按死亡次数先判。数据可用时，必须用6分钟累计英雄伤害减去0分钟基线，结合双方二人组伤害、核心补刀/经验、补给压力、击杀和离线情况判断是有效压制、无效刷伤害还是送血换血。

### 7. 装备与Aegis事实纪律

`purchase_log` 只证明物品在记录时间购买，不能单独证明已经送达、位于主背包或能立即使用。`item_uses` 只证明发生过使用；团战窗口内记录只能证明该窗口内使用过，没有目标字段时不得编造否决、莲花、微光、推推、虚灵刀等物品的具体作用对象。

没有冷却、背包或物品栏状态证据时：

- 可以说“该装备没有覆盖识别窗口”
- 可以评价购买时间是否错过强势期
- 不得直接说“有BKB故意不开”“有刷新不用”或“物品已经送达却不用”

每代Roshan必须分开记录击杀方、Aegis事件类型、持有者和最终受益方。不得把Roshan击杀方自动当成Aegis持有方；`AEGIS_STOLEN` 必须明确写成“击杀方打死Roshan、另一方抢到Aegis”。盾命、实死或复活后的战斗无法确认时必须降低证据等级。

关键装备只有完成以下因果链才算分析过：

`购买时间 → 获得的新能力或取舍 → 第一波相关战斗 → 使用/未覆盖证据 → 击杀、存活或地图结果`

具体选择标准、BKB和Aegis模板见 [equipment-review.md](references/equipment-review.md)。

### 8. 观点修正

用户提出新的比赛体验或反证时，重新检查相关窗口。新证据推翻旧判断，应明确修正，不维护错误结论。

## 输出方式

- 先给最重要的比赛结论，再给因果证据
- 不倾倒原始 JSON，不机械逐项念数据
- 完整复盘按 `full-review.md` 出稿
- 专项追问直接深挖当前问题，不从比赛概览重新开始
- 用“明显失误、低级错误、输出责任缺失、保枪倾向、团战脱节、无意义暴毙”等词时，紧跟具体时间点和后果
- 不为显得公平而平均分锅；责任比例表达相对责任，不是假装精确计算
- 数值化结论必须在首次出现时标明性质：`比赛统计`、`模型判断`或`经验估计`。几几开、责任比例和表现评分默认属于模型判断，不能伪装成数据统计

完整复盘不得把六核独立审计、关键技能矩阵或决定性团战压缩成一句总评或只有结论的简表。内容过长时按提交协议分段交付；全部章节完成前必须标记“复盘进行中”，不得宣布完成。

## 复盘完成后的发布交接

当本场复盘的 Markdown 总结已经写入原工作目录，并且用户希望发布到个人网站时，原复盘会话只负责完成和检查 Markdown，不直接执行服务器上传。此时在回复末尾明确提示用户新建一个 Work 或 Chat 会话，使用 `gpt-5.6-luna`、推理强度 `max`，并在新会话中输入“上传复盘”或“更新网页”完成最后发布。

提示内容必须带上：Match ID、Markdown 文件的绝对路径、目标网址 `https://ashfury.cn/dota/`，以及“先读取并脱敏检查，不重做整场复盘；发布后返回线上链接”的要求。新会话缺少原对话上下文，因此不能只给“上传刚才那篇”，必须给出文件路径和比赛 ID。

推荐提示文本：

> 上传复盘。Match ID：`<match_id>`；待发布 Markdown：`<absolute_path>`；目标：`https://ashfury.cn/dota/`。请先读取 Markdown，保留原复盘结论，不重做整场分析；完成脱敏、部署和线上验收后返回公开链接。当前会话使用 `gpt-5.6-luna`、推理强度 `max`。

如果 Markdown 尚未完成、证据等级未标明或用户没有明确要求公开发布，只说明尚未进入发布环节，不创建发布会话提示。

## 完成标准

提交前确认：

1. 最终结论与引用的数据一致
2. 对线、关键技能、六核、关键团和用户决策没有缺项
3. 每个强结论都能追溯到时间点、曲线、事件、目标或职责
4. 事实、推断和未知已经分开
5. 建议说明“下一次何时、看到什么信号、应该做什么”

任何一项未完成时，不得称为完整复盘：必须继续补齐，或把交付明确标记为“部分复盘”并列出无法完成的项目。

最重要的顺序始终是：

`先确定事实，再解释原因，最后评价责任。`
